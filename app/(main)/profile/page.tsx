import ProfileClient from "@/components/profile/ProfileClient";

export const metadata = { title: "Profilim | Iňlis Dili" };

export default function ProfilePage() {
  return (
    <div className="max-w-2xl mx-auto py-2">
      <ProfileClient />
    </div>
  );
}
